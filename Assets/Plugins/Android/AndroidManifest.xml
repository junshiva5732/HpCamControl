<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-feature android:name="android.hardware.microphone" android:required="false" />
  <!-- For the "incoming call" style wake-up notification (WakeUpMessagingService) -->
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <application android:theme="@style/UnityThemeSelector" android:icon="@mipmap/app_icon" android:label="@string/app_name" android:networkSecurityConfig="@xml/network_security_config">
    <!--
          Firebase Messaging requires the launcher Activity to be
          com.google.firebase.MessagingUnityPlayerActivity (a subclass of
          UnityPlayerActivity that Firebase's build preprocessor auto-generates into
          Assets/Plugins/Android/MessagingUnityPlayerActivity.java on every Android
          build - see Assets/Firebase/Editor/Messaging/FirebaseMessagingActivityGenerator.cs).
          android:turnScreenOn + android:showWhenLocked let the full-screen-intent
          notification from WakeUpMessagingService actually wake the screen and show
          over the lock screen, like an incoming call.
        -->
    <!--
          screenOrientation is sensorLandscape, not fullSensor: Player Settings
          only allows Landscape Left/Right (portrait is off), so fullSensor let
          Android rotate the window into portrait whenever the device was held
          that way while Unity's own canvas/input stayed landscape-only - visible
          content ended up sideways and, worse, touches landed on the wrong
          on-screen element entirely since the UI's hit-test coordinates and the
          rotated visual layout no longer agreed (this is what made
          "카메라전환" clicks silently do nothing while it was propped upright).
        -->
    <activity android:name="com.google.firebase.MessagingUnityPlayerActivity" android:label="@string/app_name" android:screenOrientation="sensorLandscape" android:launchMode="singleTask" android:turnScreenOn="true" android:showWhenLocked="true" android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density" android:theme="@style/UnityThemeSelector">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
    </activity>
    <service android:name="com.hpcamcontrol.WakeUpMessagingService" android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>
  </application>
</manifest>